import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  userImage: {
    width: 100,
    height: 100,
    borderRadius: 50,
    padding: 50,
    marginBottom: 0,
  },
});

export default styles;
